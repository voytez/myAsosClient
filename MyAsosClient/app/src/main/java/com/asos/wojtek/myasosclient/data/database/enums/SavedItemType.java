package com.asos.wojtek.myasosclient.data.database.enums;

/**
 * Enum constants for item types saved in database as they are saved into the same structure
 */
public enum SavedItemType {

    BAG,
    WISHLIST
}
